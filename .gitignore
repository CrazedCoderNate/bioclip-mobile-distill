# Datasets, cached teacher embeddings, checkpoints, exports.
# All far too large for git.
data/
out/
*.npy
*.parquet
*.pt
*.onnx
*.onnx.data
*.tflite

# Staged HuggingFace upload, built by prepare_release.py.
release/

# Prefetched encyclopedia, built by s08 from Wikidata/Wikipedia. Regenerable,
# and this repo ships code, not derived data. The curated_safety.csv seed that
# feeds into it IS tracked; the fetched result is not.
species_facts.json

.venv/
__pycache__/
*.pyc
